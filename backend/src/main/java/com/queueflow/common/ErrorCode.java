package com.queueflow.common;

/** Stable API error codes shared by controllers, services and exception handlers. */
public enum ErrorCode {
    ACCESS_DENIED,
    INTERNAL_ERROR,
    RESOURCE_NOT_FOUND,
    VALIDATION_ERROR
}
